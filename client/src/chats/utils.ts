import type { ApplicationCommandStructure } from 'eris'

import type { ClientType } from '../libs/eris.js'

import { config } from '../configs/index.js'

// export const createCommandInitializer = (bot: ClientType, struct: ApplicationCommandStructure) => {
//   return () => bot.createGuildCommand(config.DISCORD_DEFAULT_GUILD_ID, struct)
// }
